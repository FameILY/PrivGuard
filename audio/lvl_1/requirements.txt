# Updated requirements - PyTorch 2.5.1 for WhisperX compatibility
# Works on Windows Python 3.9 without build tools

# Core transcription
whisperx

# PyTorch (2.5.1 - compatible with WhisperX)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1
torchaudio==2.5.1

# NER (using transformers only, no spaCy)
transformers

# Audio processing
pydub

# Utilities
numpy
scipy

# Note: This version uses BERT for NER instead of spaCy
# Run after installation:
# python src/main.py -i input/audio.wav -o output/redacted.wav